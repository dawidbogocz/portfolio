<div class="section-header">
    <h2 class="section-title">About</h2>
    <p class="section-subtitle">A bit about me</p>
</div>
<div class="about-grid">
    <div class="about-code">
        <div class="code-line"><span class="code-keyword">public</span> <span class="code-keyword">class</span> <span class="code-type">Developer</span></div>
        <div class="code-line code-brace">{</div>
        <div class="code-line indent-1"><span class="code-keyword">public string</span> Name { <span class="code-keyword">get;</span> } = <span class="code-string">"Dawid Bogocz"</span>;</div>
        <div class="code-line indent-1"><span class="code-keyword">public string</span> Alias { <span class="code-keyword">get;</span> } = <span class="code-string">"DzeyD"</span>;</div>
        <div class="code-line indent-1"><span class="code-keyword">public string</span> Location { <span class="code-keyword">get;</span> } = <span class="code-string">"Poland"</span>;</div>
        <div class="code-line indent-1"><span class="code-keyword">public string</span> Education { <span class="code-keyword">get;</span> } = <span class="code-string">"SUT, Computer Science"</span>;</div>
        <div class="code-line indent-1"><span class="code-keyword">public string</span> Role { <span class="code-keyword">get;</span> } = <span class="code-string">"MES Administrator"</span>;</div>
        <div class="code-line indent-1"><span class="code-keyword">public string</span> Company { <span class="code-keyword">get;</span> } = <span class="code-string">"Maspex"</span>;</div>
        <div class="code-line indent-1"><span class="code-keyword">public string[]</span> Passions { <span class="code-keyword">get;</span> } = [<span class="code-string">"gaming"</span>, <span class="code-string">"game dev"</span>, <span class="code-string">".NET"</span>, <span class="code-string">"AI"</span>];</div>
        <div class="code-line code-brace">}</div>
    </div>
    <div class="about-text">
        <p>I'm a Computer Science graduate from Silesian University of Technology, currently working as an MES Administrator at Maspex, Poland's largest food company. My day job involves keeping manufacturing execution systems running smoothly and helping users across the organization.</p>
        <p>Outside of work, I'm passionate about game development, .NET, and exploring AI/ML technologies. I'm always building something -- whether it's a game, a developer tool, or experimenting with the latest tech.</p>
    </div>
</div>